export * from './types';
export * from './mockData';
export { default as StatsCard } from './StatsCard';

// Export the renamed interface explicitly to avoid confusion
export type { AdvertisementFormData } from './types';
